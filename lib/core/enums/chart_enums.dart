enum ChartLegendPosition { top, bottom, left, right, auto }

enum ChartAxisType { numeric, category, dateTime, logarithmic }

enum ChartAnimationType { none, linear, easeIn, easeOut, easeInOut }

enum ChartInteractionMode { none, tooltip, trackball, crosshair }

enum ChartSelectionMode { point, series, cluster }

enum ChartExportFormat { png, pdf, excel }
