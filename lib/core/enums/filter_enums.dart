enum DateRangeFilter { today, thisWeek, thisMonth, thisQuarter, thisYear, custom }

enum RegionFilter { all, northAmerica, europe, asia, latinAmerica, africa, oceania }

enum DepartmentFilter { all, sales, marketing, engineering, hr, finance, operations }

enum StatusFilter { all, active, inactive, pending, completed, cancelled }
